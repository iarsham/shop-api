package constans

const (
	TokenExpired      string = "token expired"
	TokenInvalid      string = "token invalid"
	RefreshNotAllowed string = "refresh token not allowed"
	TokenType         string = "bearer"
)
